export type SocialLink = {
  href: string;
  label: string;
};

export type TimelineEntry = {
  date: string;
  title: string;
  content: string;
};

export type TOC = {
  depth: number;
  text: string;
  slug: string;
};
